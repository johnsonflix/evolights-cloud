# Generated by `mosquitto_passwd -c passwd <user>`.
# Bootstrap with the api service user; per-device users are added at pairing time
# by the api (writing through a future plugin or by running `mosquitto_passwd` over an exec).
#
# Placeholder — replace with a real hashed entry before starting:
#   docker run --rm -v $PWD/mosquitto:/m eclipse-mosquitto:2 \
#     mosquitto_passwd -b /m/passwd api change-me
